<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.conectinglaw">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.ListadoAbogadosActivity"></activity>
        <activity android:name=".view.SeleccionarCasoActivity" />
        <activity android:name=".view.ChatActivity" />
        <activity android:name=".view.fragment.ProfileClientFragmentActivity" />
        <activity android:name=".view.fragment.ChatFragmentActivity" />
        <activity android:name=".view.fragment.ProfileUserFragmentActivity" />
        <activity
            android:name=".view.ContainerActivity"
            android:theme="@style/AppThemeActionBar" />
        <activity android:name=".view.CreateClientAccountActivity" />
        <activity android:name=".view.CreateLawyerAccountActivity" />
        <activity android:name=".view.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>